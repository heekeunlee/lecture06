{
  "name": "lecture06",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "predeploy": "mkdir -p dist && cp -f lecture06_script.pdf dist/ && cp -f index.html dist/",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
